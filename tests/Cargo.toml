# Integration Tests for Pyra Compiler
# These tests verify the end-to-end functionality of the compiler

# Test configuration
[test]
name = "integration_tests"
path = "tests/integration_tests.rs"

# Dependencies for testing
[dev-dependencies]
tokio = { version = "1", features = ["full"] }
assert_cmd = "2.0"
predicates = "3.0"
tempfile = "3.0"